<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="human.class1.ajax.mapper.FmMapper">
    <select id="getProcesses" resultType="human.class1.ajax.dto.FmDTO">
        SELECT process_code AS processCode,
               process_name AS processName,
               process_category AS processType,
               product_code AS productCode,
               is_used AS useYn
        FROM PROCESS
    </select>
</mapper>